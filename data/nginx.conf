server {
  listen 80;
  
  root /usr/share/nginx/html/;

  gzip on;
  gzip_types *;
  gzip_proxied any;
  
  sendfile on;
  tcp_nopush on;
  tcp_nodelay on;
  keepalive_timeout 65;

  location / {
    autoindex on;
  }
 }